import { BaseController } from '../common/base.controller';
import { inject, injectable } from 'inversify';
@injectable()
export class FilesController extends BaseController {
	// constructor(@inject(TYPES.File)) {
	//     super();
	// }
	// async uploadFile() {
	//
	// }
}
